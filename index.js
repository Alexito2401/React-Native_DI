/**
 * @format
 */

import {AppRegistry} from 'react-native';
import Partido from './Screens/Partidos';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => Partido);
